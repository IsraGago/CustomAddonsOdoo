# Crear módulo con scaffold usando la ruta absoluta al Python del entorno virtual
/opt/odoo/odoo-env/bin/python3 /opt/odoo/odoo/odoo-bin scaffold prueba /opt/odoo/custom-addons/
# Cambiar el dueño de los módulos para que odoo pueda leerlo
sudo chown -R odoo:odoo /opt/odoo/custom-addons
# encontrar el fichero de configuracion
sudo find /opt/odoo -name odoo.conf | grep odoo.conf
# meter el usuario alumno al grupo de odoo (hay que reiniciar la sesión)
sudo usermod -aG odoo alumno
#ver los grupos a los que pertenece un usuario
groups alumno
#dar permisos al grupo
sudo chmod -R 775 /ruta